[package]
name = "promoforge-ingestion"
version = "0.1.0"
edition = "2021"

[dependencies]
promoforge-core = { path = "../core" }
walkdir = "2"
ignore = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
