[package]
name = "promoforge-pkb"
version = "0.1.0"
edition = "2021"

[dependencies]
promoforge-core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }
tracing = "0.1"
